# 宿主机回环端口；外部流量应始终由宿主机 Nginx 转发。
TWILIGHT_LEAP_PORT=3100

# 构建时写入 Open Graph 等绝对链接。正式部署时必须改为实际 HTTPS 域名。
NEXT_PUBLIC_SITE_URL=https://game.example.com

# 可选：自定义本地镜像标签。
TWILIGHT_LEAP_IMAGE=twilight-leap:local
